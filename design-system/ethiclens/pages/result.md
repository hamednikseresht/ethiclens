# Result page — override

Master still applies: Swiss/minimal, Shabnam, ink primary, no clay, four app tabs unchanged.

## This page

- The recommendation is a **pull-quote** (hairline start border, display verdict, body clamped with «ادامه»). Do not restore the inverted black slab (`bg-foreground` + `text-background`).
- Long sections live in four **in-page tabs** (خلاصه / سنجش / استدلال / عمل) plus an accordion. This is progressive disclosure, not a second nav.
- Truncate with expand (`line-clamp-3` + button). Do not dump all 26 blocks open.
- Touch floor 44px on tab triggers, accordion headers, and expand controls.
- Motion 150ms; honour `prefers-reduced-motion`.
