# design/

Live UI is in `client/` and `public/`. Historical mobile PWA mockups (HTML
prototypes, an unsafe service worker) sit in `docs/archive/mobile-pwa/`.
Do not copy that worker into production.
